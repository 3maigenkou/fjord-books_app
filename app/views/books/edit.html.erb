<h1><%= Book.human_attribute_name 'editing_book' %></h1>

<%= render 'form', book: @book %>

<%= link_to t('activerecord.attributes.function.show'), @book %> |
<%= link_to t('activerecord.attributes.function.back'), books_path %>
